# Tier 2 Basic First-Wire Caveats

- OpenClaw: confirm on first wire that `${VAR}` interpolation is honored inside `mcp.servers`.
- CodeWhale: confirm on first wire whether an `mcp_config_path` overlay can replace `DEEPSEEK_MCP_CONFIG`.

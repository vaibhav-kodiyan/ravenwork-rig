{
  "name": "rig-agent-toolbox",
  "version": "4.8.4",
  "private": true,
  "description": "A curated, host-agnostic markdown toolbox for coding agents.",
  "keywords": ["rig", "agent", "skills", "claude", "cursor"],
  "license": "MIT",
  "scripts": {
    "setup:dev": "python3 -m venv .venv && .venv/bin/python -m pip install -r requirements-dev.txt",
    "pretest": "PATH=.venv/bin:$PATH python3 -c \"import pandas\" 2>/dev/null || npm run setup:dev",
    "test": "PATH=.venv/bin:$PATH node --test tests/*.test.js && npm test --prefix pi-extension",
    "test:rig": "node --test tests/rig-bootstrap.test.js"
  }
}
